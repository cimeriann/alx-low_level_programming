#include "main.h"

/**
 * sub - checks for a number
 * @a: digit to be checked
 * @b: digit to be checked
 * Return: the multiplicationof the two int s
 */

int sub(int a, int b)
{
	return (a - b);
}
