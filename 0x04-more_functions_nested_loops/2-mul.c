#include "main.h"
#include <stdio.h>

/**
 * mul - checks for a number
 * @a: digit to be checked
 * @b: digit to cbe checked
 * Return: 0
 */
int mul(int a, int b)
{
	int times;
	
	times = a * b;

	return (times);
}
