#include "main.h"

/**
 * _abs - print the absolute value of a number
 * Return - int
 */

int _abs(int)
{
	int soln;

	if (int < 0)
	{
		soln = int * -1;
		_putchar(soln);
		return (1);
	}
	else
	{
		_putchar(int);
		return (0);
	}
}
